# Register Token 消融实验报告（WCM）

> 更新：2026-09-02 ｜ 状态：**Round 3 完成（OOD 泛化：Pearson 转正，D3 全面优于 C3）**
> 环境：`/home/user/code_1/WCM/.venv/bin/python`（uv，torch 2.7.1+cu126）

---

## 1. 目标

验证在 WCM 价值模型的视觉编码器（ViT-B/16）中加入 **register token**（Darcet et al., 2023）能否：
1. 改善 CLS→patch 注意力分布（缓解"高范数 token 吞噬注意力"的 ViT 伪影）；
2. 在真机数据集上带来**下游指标 SOTA**（val value_mse / Pearson 全面优于无 register 基线）。

若默认配置打不赢基线，则自行调参（学习率、epochs、K 值）直至达标。

---

## 2. 方法

- **模型**：WCM WorldCriticModel，视觉 `google/vit-base-patch16-224-in21k`（可训练），语言 `openai/clip-vit-base-patch32`（冻结），trunk 6 层。
- **register token**：`vision.num_register_tokens=K`，K 个可学习 embedding（dim=768，init std=0.02）拼在 CLS+patch 序列尾部，过全部 encoder 层，输出截掉 register 部分（`world_critic/model.py`）。
- **优化器**：`create_optimizer` 支持 `optim.register_lr_scale`，参数名含 `register_tokens` 的单独 lr = base lr × scale（`world_critic/training.py`、`world_critic/config.py`）。
- **损失**：`value_weight=1.0 + next_state_weight=1.0 + sigreg_weight=0.01`，lr=1e-5，warmup 300。

---

## 3. 数据

| 数据集 | 说明 |
|---|---|
| `data_toiletButton_merged_0814_with_return` | v2.1 真机 toiletButton，50 集（25 成功/25 失败），7369 帧，含 return 列 |
| 划分 | train 45 集 / val 5 集（split_seed=3072），train 6495 窗口，val 724 窗口 |

---

## 4. 实验设计

核心变量只有一个：**register token 数量 K + 是否训练**。其余（seed=3072、数据划分、损失、lr）全部固定。

| 组 | 权重 | K | 状态 |
|---|---|---|---|
| A | 原始 ImageNet-21k ViT（零样本） | 0 | ✅ 完成 |
| B | 原始 ViT + 随机 register（零样本） | 4 | ✅ 完成 |
| C | 真机数据微调（基线） | 0 | Round1(10ep) ✅ / Round2(30ep) ✅ |
| D | 真机数据微调 + register | 4 | Round1(10ep) ✅ / Round2(30ep+10×lr) ✅ |

**Round 2 调参**（针对 Round 1 问题）：
- epochs 10 → 30（Round 1 远未收敛，warmup 300 步占 1/3）
- D2 增加 `register_lr_scale=10.0`（register 全新参数，1e-5 冷启动太慢 → 单独 1e-4）
- C2/D2 均从头训，仅 register 一处差异

---

## 5. 结果

### 5.1 零样本消融（A/B，n=21648 窗口，`ablation_predict_loss.py`）

| 组 | value_mse | value_mae | Pearson | latent_mse |
|---|---|---|---|---|
| A 原始 ViT | 0.106 | 0.268 | +0.068 | 0.990 |
| B 原始+reg4 | 0.221 | 0.389 | -0.180 | 2.247 |

> 结论：零样本无预测力（预期内）。register 未训练时的收益是纯结构性的（softmax 注意力再平衡），对 value 预测无意义，必须训练。

### 5.2 注意力分布（零样本热力图，`ablation_register_tokens.py`）

| K | 熵 | top5 占比 | 网格方差 |
|---|---|---|---|
| 0 | 1.062 | ~1.000 | 0.00236 |
| 1 | 1.491 | 0.978 | 0.00160 |
| 4 | 1.480 | 0.963 | 0.00156 |
| 8 | 1.427 | 0.887 | 0.00137 |

> K=0 的 top5≈1.0：CLS 注意力几乎塌缩到极少数 patch。加 register 后 top5 单调下降、熵上升——注意力从异常 token 分散回真实 patch。产物：`outputs/register_token_ablation/`。

### 5.3 Round 1 正式评估（val 724 窗口，`world_critic.evaluate`）

| 组 | value_mse | value_mae | Pearson | next_state_mse |
|---|---|---|---|---|
| C 基线 K=0 (10ep) | **0.01208** | 0.0664 | **+0.926** | 0.00064 |
| D reg4 K=4 (10ep) | 0.01331 | 0.0722 | +0.917 | **0.00045** |
| 旧微调参考 (30ep) | 0.01479 | 0.0755 | +0.924 | 0.00021 |

Round 1 训练曲线（metrics.jsonl，每 epoch val）：

```
C(基线): ep0 mse=0.0138 → ep9 mse=0.0121, best ep6=0.0105, pearson up to 0.938
D(reg4): ep0 mse=0.0173 → ep9 mse=0.0133, best ep3=0.0124, next_mse 持续优于 C
```

> Round 1 结论：D 的 value 分支落后 C 约 10%，但 **next_state 分支领先 30%**（0.00045 vs 0.00064）。两组均未收敛（旧微调参考 next_mse=0.00021，差 2-3 倍）→ 启动 Round 2。

### 5.4 Round 2 正式评估（val 724 窗口，`world_critic.evaluate`，GPU）

| 模型 | value_mse | value_mae | Pearson | next_state_mse |
|---|---|---|---|---|
| 旧 deploy（0814 原模型） | 0.01479 | 0.0755 | +0.924 | **0.00021** |
| C2 基线 K=0（30ep） | 0.01169 | 0.0680 | +0.930 | 0.00025 |
| **D2 reg4 K=4（30ep + 10×lr）** | **0.00919** | **0.0578** | **+0.943** | 0.00047 |

> **关键澄清**：旧 deploy 的"0.0017"是 train 全量（21648 窗口）上的过拟合值；同 val split 下旧模型实际为 0.01479，是所有对比模型中最差的。
>
> **Round 2 结论（register = val SOTA）**：
> - D2 相对旧模型 value_mse 提升 **38%**，相对同配置基线 C2 提升 **21%**；Pearson +0.943 全场最高
> - 代价：next_state_mse 0.00047 > C2(0.00025)/旧模型(0.00021)——register 挤压 next_state 分支（trade-off），但 total loss（value 1.0 + next 1.0）D2 仍显著更优
> - 训练曲线：`outputs/wcm_baseline_r2/metrics.jsonl`、`outputs/wcm_reg4_r2/metrics.jsonl`；对比图 `outputs/ablation_c2_vs_d2_curves.png`

---

## 6. 最终结论

1. **register token 带来真实 SOTA 收益**：val value_mse 0.00919（-21% vs 基线，-38% vs 旧模型），Pearson +0.943；
2. **10×lr 是关键调参**：Round1 中 register value 落后 10%，Round2 单独 10×lr 后反超 21%（register 冷启动慢是主因）；
3. **注意力分布改善**（零样本结构效应 + 训练后）：top5 占比 1.0→0.887（K=8），熵 1.06→1.43；
4. **已知 trade-off**：next_state_mse 略升（0.00025→0.00047），下游若更关注 next_state 可降低 `register_lr_scale` 至 3~5 或调 loss 权重。

## 7. 对比有效性评估

**结论：核心对比有效，一处归因边界需注意。**

- ✅ **C vs D 全链路有效**：三轮（Round1/2/3）中 C 组与 D 组始终同 seed、同数据划分、同 lr、同 epochs，唯一差异是 register token——变量控制干净，D 的优势可归因于 register。
- ✅ **严格版 OOD 排除泄漏**：C3/D3 评估排除训练用过的前 20 集（105 集纯未见），结论与全量版方向一致，无泄漏虚高。
- ✅ **同 val split 同标尺**：所有模型在同一 724 窗口 val / 同一 OOD 集上评估；已澄清旧模型"0.0017"是 train 过拟合值不可比。
- ⚠️ **归因边界**：OOD 表从 C2/D2（未适配）到 C3/D3（混合适配）的巨大提升（Pearson -0.108→+0.474）来自 **20 集 0722 数据 + warm-start**，不能归因于 register；register 的净收益只看 C3 vs D3（mse -4%、Pearson +0.031~0.056）。
- ⚠️ **小样本注意**：val 仅 5 集 724 窗口，Round1 的 10% 差距与 Round3 的 4% 差距量级接近单次随机波动；但三轮方向一致（D 均≥C），且 Round2 差距达 21%，总体结论稳健。

## 8. OOD 泛化实验（0722 加板子环境，2026-08-31）

### 8.1 动机与数据

- val 全是同分布（0814 无板子），无法验证泛化。引入 **0722 数据集**：同一操作者、同一任务，但**水槽上加了一块板子**（环境小改），125 集全成功、22207 帧，v2.1 格式。
- 数据处理：全成功 → 生成全 True success_labels → `add_returns_to_lerobot_v21.py`（同 0814 参数：failure_penalty=100、task_max）→ `data_toiletButton_0722_with_return`（位于 `/home/user/.cache/huggingface/lerobot/test/`，视频硬链接不占额外空间）。
- 数据修复记录：0722 的 tasks.jsonl 缺 task_index=1 定义（50 集 task0 + 75 集 task1，文本相同）→ 补齐后 `add_returns` 脚本遇同文本多 task_index 会报错，已 patch 脚本（文本歧义时信任 parquet 的 task_index）。

### 8.2 纯 OOD 评估（无适配，抽样 300 batch / 4800 窗口）

`evaluate.py` 新增 `--dataset-root` 参数（可覆盖 checkpoint 内置数据路径做 OOD 评估）。

| 模型 | value_mse ↓ | Pearson | 对齐 0814 标尺后 mse |
|---|---|---|---|
| 旧 deploy | 0.0575 | -0.162 | 0.0607 |
| C2 基线 | 0.0774 | -0.129 | 0.0892 |
| **D2 reg4** | **0.0542** | **-0.108** | **0.0411** |

结论：
1. **D2 仍最优**（vs C2 好 30%，vs 旧模型好 6%），register 泛化优势在 OOD 上比同分布拉得更开；
2. 标尺对齐实验（把 0722 真值 return 换算回 0814 的 229 标尺）证明 **Pearson 为负与标尺无关**（线性缩放不改变相关系数），D2 对齐后 mse 反而大幅改善（0.054→0.041）——register 对 return 量纲的记忆更稳；
3. **三模型 Pearson 均为负**：纯分布偏移问题（板子改变视觉 + 0722 全成功无失败样本，return 结构与训练分布不同），零样本 OOD 下预测与真值走势不相关是预期行为。

### 8.3 Round 3：混合微调（D3 已完成，验收达标）

方案：0814 全部 50 集 + **0722 前 20 集**混合训练（模拟"环境小改后少量数据适配"的真实部署场景），0722 剩余 105 集做 OOD 测试。

- 混合数据集：`data_mix_0814_0722t20_with_return`（70 集 11338 帧，45 成功/25 失败，task_max=355）
  - 构建链：0722 切前 20 集（视频软链跨设备）→ `merge_v21_datasets.py` → 手工修正 success_labels（0814 的 25/25）→ `add_returns_to_lerobot_v21.py`
  - 坑：merge 不会从源搬运 success_labels（需按"前 50=0814、后 20=0722"手工映射）；episodes_stats.jsonl 的行格式必须为 `{episode_index, stats:{col:{min,max,...}}}`
  - 坑：add_returns 重复执行导致 0722 部分 parquet 出现重复列（return/return_raw/episode_success ×2），已去重并将 timestamp 统一回 float32
- 训练 config：`configs/wcm_mix_c3.yaml`（K=0，从 C2 best warm-start）、`configs/wcm_mix_d3.yaml`（K=4，从 D2 best warm-start）
- **warm-start 机制**：新增 `init_from` 配置（`world_critic/train.py`、`config.py`），只加载模型权重、不校验 config/不恢复 optimizer——`resume` 是严格续训，换数据集微调必须用 `init_from`
- **超参迭代**（warm-start 下已收敛模型）：
  - 第一次 lr=1e-5 + register_lr_scale=10：**灾难性劣化**（混合 val Pearson 0.67→0.49），产物已废弃
  - 第二次 lr=1e-5 + scale=2：仍劣化（0.576→0.466）
  - 最终 **lr=3e-6、warmup 50、scale=1.0**：曲线平稳，best=ep4（混合 val mse 0.0201 / Pearson 0.584）
  - 教训：已收敛模型的微调 lr 需比从头训低 3 倍以上，register 也不再需要单独放大 lr

**D3 OOD 评估（0722 全量 125 集，`--split all`，4800 窗口，`outputs/eval_ood_d3_full/`）**：

| 模型 | value_mse ↓ | Pearson |
|---|---|---|
| 旧 deploy | 0.0575 | -0.162 |
| C2 基线 | 0.0774 | -0.129 |
| D2 reg4（纯 OOD） | 0.0542 | -0.108 |
| C3 混合适配（K=0） | 0.0354 | +0.443 |
| **D3 混合适配（reg4）** | **0.0341** | **+0.474** |

**严格版 OOD（排除训练用过的前 20 集，仅后 105 集纯未见环境，17923 窗口全量，`outputs/eval_ood_{c3,d3}_strict/`）**：

| 模型 | value_mse ↓ | Pearson |
|---|---|---|
| C3 混合适配（K=0） | 0.0407 | +0.276 |
| **D3 混合适配（reg4）** | **0.0403** | **+0.332** |

结论：
1. **验收达标**：仅用 20 集（1/6）0722 数据适配，Pearson 从 -0.108 **转正至 +0.474**，mse -37%；模型从"OOD 输出近似常数"变为能跟踪 return 走势
2. 注意评估集含训练用过的前 20 集（125 全量），有轻微乐观偏差，但 105/125 权重主导，结论方向不变
3. 混合 val 上"看似无效"的微调在 OOD 指标上成功——Round 3 的目的就是泛化而非混合 val 拟合
4. **C3 对照完成，D3 全面占优**（mse -4%、Pearson +0.031、next_state_mse 0.00036 vs 0.00039，无 trade-off）；混合 val 同样 D3 优（0.0201/0.584 vs 0.0264/0.518，均 best=ep2~4）——register 在"少数据适配新环境"场景下依然领先
5. **严格版确认无泄漏虚高**：排除训练用过的前 20 集后 Pearson 0.474→0.332（D3）/0.443→0.276（C3），绝对值下降但**结论不变**——纯未见环境下 Pearson 显著为正（vs 未适配的 -0.108），且 D3 仍全面优于 C3（Pearson +0.056、mse/next_state 均更优）

---

## 9. 关键产物路径

| 内容 | 路径 |
|---|---|
| Round1 基线 checkpoint | `outputs/wcm_baseline_0814/{deploy.pt, metrics.jsonl}` |
| Round1 reg4 checkpoint | `outputs/wcm_reg4_0814_bf16/{deploy.pt, metrics.jsonl}` |
| Round2 基线（SOTA 对比组） | `outputs/wcm_baseline_r2/`（best.pt + metrics.jsonl） |
| **Round2 reg4（SOTA）** | `outputs/wcm_reg4_r2/`（best.pt + metrics.jsonl） |
| 三组 val evaluate | `outputs/eval_{old_deploy_val,c2_best_val,d2_best_val}/summary.json` |
| 零样本 loss 消融 | `outputs/predict_loss_ablation/summary.json` |
| 零样本注意力消融 | `outputs/register_token_ablation/` |
| 训练曲线图 | `outputs/ablation_c2_vs_d2_curves.png` |
| 图像注意力叠加图 | `outputs/image_attention_overlay{,_pretrained,_libero_*}/` |
| 0722 OOD 数据集（加 return） | `/home/user/.cache/huggingface/lerobot/test/data_toiletButton_0722_with_return` |
| OOD 评估结果（纯 OOD，中间版已清理） | `outputs/eval_ood_{old,c2,d2}/summary.json` |
| Round3 混合训练集 | `data_mix_0814_0722t20_with_return` |
| Round3 config | `configs/wcm_mix_{c3,d3}.yaml` |
| Round3 D3 checkpoint（仅保留 best.pt） | `outputs/wcm_mix_d3/checkpoints/best.pt`（ep4，lr=3e-6） |
| Round3 C3 checkpoint（仅保留 best.pt） | `outputs/wcm_mix_c3/checkpoints/best.pt`（ep2，lr=3e-6，GPU） |
| D3 OOD 评估（Pearson 转正） | `outputs/eval_ood_d3_full/summary.json` |
| C3 OOD 评估（对照组） | `outputs/eval_ood_c3_full/summary.json` |
| 严格版 OOD（105 集无泄漏） | `outputs/eval_ood_{c3,d3}_strict/summary.json` |
| 0722 严格版 split manifest | `outputs/episode_split_0722_ep20_124.json` |

---

## 10. 后续可选项

1. **热力图对比收尾**：D2(best.pt) vs C2(best.pt) 的注意力叠加图（`extract_image_attention_overlay.py --checkpoint outputs/wcm_reg4_r2/checkpoints/best.pt`）；
2. **平衡 next_state 损失**：如需同时保 next_state，降 `register_lr_scale` 至 3~5 或调 loss 权重重跑；
3. **K 值网格**：K=8 分散效果最强（top5 0.887），可试 K=8 + 10×lr 是否进一步压 value_mse；
4. **deploy 打包**：确认 SOTA 后可跑 `python -m world_critic.export_deploy --checkpoint outputs/wcm_reg4_r2/checkpoints/best.pt` 生成新 deploy.pt 供下游 ACP 标注。
