# 寄存器实验重构路线

更新：2026-09-11

## 当前判定

现有结果不能支持“寄存器提升 WCM 价值预测”。它们能支持的结论较窄：旧 `late`
空间寄存器实验混入 ViT 双倍深度；严格因果的 `bs1` 在两个扩训 seed 上方向翻转；
扩训 baseline 的改善大于已观察到的结构差异，但原因尚未被隔离。

“数据量是真增益”也需要撤回为待验证假设。扩训同时加入 0814 来源的 77 个全成功
episode 和 0703 无板子来源的 50 个全失败 episode，采集来源、场景和成功标签高度耦合。
这次改动同时改变数据量、域分布和类别比例，不能把 baseline 改善单独归因于样本数。

报告中的“数绝对帧号”解释也不成立。数据管线虽然记录 `frame_indices`，模型前向只接收
图像、指令、动作和 `valid_mask`，价值分支在动作进入计算图前完成；固定三帧滑窗的
time embedding 始终是局部位置 0、1、2。除每集最初的边界样本外，模型没有绝对帧号
可数。OOD 下降更合理的候选解释是视觉域偏移、轨迹阶段线索改变或 return 标尺改变，
必须通过干预实验区分。

另一个统计问题是把 27727 个高度重叠的窗口当作独立证据。实验单位应是 episode；
窗口可用于算总体误差，但不应作为置信区间的独立样本。两个 seed 的方向翻转只能判为
证据不足，不能做显著性结论。

## 唯一研究问题

后续只回答一个问题：在必须利用视觉证据的价值任务上，标准 early ViT register 是否
相对同预算 baseline 改善跨 episode 泛化？在回答前，不再测试寄存器数量、时序寄存器、
block register 或 combo。

## 阶段 0：代码门禁

1. 记录 Linux 训练环境的精确 Transformers 版本，并加载实际
   `google/vit-base-patch16-224-in21k`，分别对 K=0 与
   K=4 early 跑一批 forward/backward；验证形状、有限值、寄存器梯度和单次 encoder
   调用。命令：`.venv/bin/python -m scripts.smoke_test_register_encoder --device cuda`。
   当前 Windows 工作区没有 torch/transformers，静态检查不能替代这一步。
2. K=0 与 K=4 必须使用相同 encoder 深度；`late` 只保留历史 checkpoint 复现。
3. 可部署实验必须 `register_block_size=1`、`register_future_leak_ratio=0`。非因果配置
   只有显式设置 `allow_noncausal_register_ablation=true` 才能运行。
4. 注意力机制指标拆成两项：`register_mass` 衡量寄存器吸收的质量；对 patch 重新归一化
   后的 `patch_top5_ratio`/entropy 衡量 patch 内部分布。原始 top5 不能单独证明伪影改善。

门禁失败就修代码，不启动训练。

## 阶段 1：任务是否需要视觉

在 baseline 上做同一批 episode 的配对干预：正常输入、整窗同图冻结、跨 episode 图像
替换、单帧模型。报告每种干预相对正常输入的 episode 级 MSE、Pearson 和预测变化。
现有 `diagnose_frame_counting.py` 可用于回拨/冻结敏感性，但其帧号分桶只是事后分析，
不能证明计数机制。

先按“采集域 × 成败”审计数据；每个采集域都应同时含成功和失败，或在统计中按域分层，
禁止让来源直接代理标签。构建包含成功和失败的独立测试集，并尽量匹配两类轨迹长度。
全成功的 5cut 只能测试
成功轨迹上的回归，无法验证最关键的成败区分。主指标改为：episode 级成功 AUC、失败
提前识别率、episode 平均 MSE；窗口总体 MSE作为辅助指标。

若视觉破坏不显著降低指标，说明任务或标签仍未迫使视觉学习，停止结构实验并先修数据与
目标。若视觉破坏稳定降低指标，进入阶段 2。

## 阶段 2：一次干净的结构对照

只训练 baseline 与 K=4 early 两组。使用完全相同的数据划分、数据顺序、训练步数、
优化器和 seed；至少 5 个配对 seed。两组都从同一预训练 ViT 开始，禁止从历史 `late`
checkpoint warm-start。固定训练步数作为主结果；若使用 best checkpoint，需先实现 EMA，
并对两组采用同一选点规则。

每次评估保留 `episode_metrics.json`。使用 `scripts/compare_experiments.py` 对同一测试集做
episode 配对 bootstrap，报告候选减 baseline 的 MSE 差、95% CI 和候选更优概率。跨 seed
再报告配对差值的均值、标准差和每个 seed 的方向，不能只汇总最佳 seed。

## 判定与停止规则

- early register 在至少 4/5 个 seed 同向改善，且 episode 配对 95% CI 排除 0，才称为收益。
- 预先设定实际意义阈值：建议 episode MSE 相对改善至少 5%，成功 AUC 提升至少 0.02。
- 机制指标改变但任务指标未过阈值，结论写成“机制生效、任务无收益”，停止 combo。
- 效果未复现时接受零结果，不再从 K、学习率、epoch 或 checkpoint 中追逐单点最优。

## 旁支结论：RankNet 排序损失（已否决）

2026-09-11 完成一组与寄存器无关的 loss-only 消融，同样判为否决。

对照设置：`wcm_rank_exp_s{3072,42}` 相对 `wcm_baseline_exp_s{3072,42}` 只增加
`ranking_weight=0.05` 的 pairwise ranking loss（window 末尾时刻做配对 logistic 比较，
`ranking_temperature=0.1`、`ranking_min_target_gap=0.1`），架构、数据划分、schedule、
seed、checkpoint 选点（第 10 轮 `deploy.pt`）完全一致。评估流程见 `20_run_rank_pair.sh`，
判定规则为预先设定：两个 seed 的 OOD Pearson 都要改善，方向相反即否决。

结果（`outputs/eval_rank_pair/comparisons/*.json`，episode 配对 bootstrap，125/145 集）：

| 指标 | 5cut s3072 | 5cut s42 | OOD s3072 | OOD s42 |
| --- | --- | --- | --- | --- |
| MSE 相对变化 | -5.6% | +1.4% | -17.3% | +58.5% |
| MSE 95% CI 排除 0 | 否 | 否 | 是（更优） | 是（更差） |
| Pearson 差 | +0.0753 | +0.0113 | -0.0113 | -0.1001 |
| Pearson 95% CI 排除 0 | 是（更优） | 否 | 否 | 是（更差） |

判否理由：OOD Pearson 两个 seed 均未改善（s3072 为噪声级负向，s42 显着变差），
OOD MSE 两个 seed 方向相反。唯一正面证据是 s3072 的 5cut Pearson（CI 排除 0），
属于分布内单 seed 的边际收益，不足以支撑跨 episode 泛化。结论为
“排序损失在分布内有边际信号、OOD 不复现”，按规则停止 RankNet 路线，不补 seed、
不再调 `ranking_weight` 或温度追单点最优。

## 下一次运行顺序

1. 在 Linux 训练机运行 `bash 19_run_reg4_early_pair.sh`。脚本先做真实 ViT 冒烟门禁，
   再训练 early-register 的 seed 3072/42，并用第 10 轮 `deploy.pt` 做 episode 配对评估。
2. 两个 seed 方向相反则提前停止寄存器路线；两个 seed 在 5cut/OOD 均改善才补另外三个
   baseline/early 配对 seed。两 seed 预检只用于停止或继续，不能用于发布显著性结论。
3. 同时完成 baseline 视觉干预诊断与“采集域 × 成败”的混合测试集审计。
4. 满足视觉依赖门槛并完成 5 seed 后，用 episode 配对 bootstrap 作最终判定。
